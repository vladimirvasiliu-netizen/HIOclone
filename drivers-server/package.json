{
  "name": "drivers-server",
  "version": "1.0.0",
  "description": "Server Express standalone pentru curieri (date in memorie)",
  "main": "server.js",
  "scripts": {
    "dev": "node --watch server.js",
    "start": "node server.js"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "express": "^4.19.2"
  }
}
